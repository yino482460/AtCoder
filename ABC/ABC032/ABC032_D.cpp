#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
  /* 入力 */
  long N, W;
  cin>> N>> W;
  vector<long> n(N), w(N);
  for (size_t i = 0; i < N; i++)
  {
    cin>> n[i]>> w[i];
  }

  /* 動的計画法 */


  return 0;
}
