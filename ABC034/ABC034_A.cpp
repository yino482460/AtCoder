#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
  /* 入力 */
  int x, y;
  cin >> x>>y;

  // 出力
  if (x < y)
  {
    cout << "Better" << endl;
  }
  else
  {
    cout << "Worse" << endl;
  }

  return 0;
}
