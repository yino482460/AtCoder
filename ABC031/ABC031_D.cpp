#include <iostream>
#include <vector>
using namespace std;

int main(int argc, char const *argv[])
{
  // 入力
  long K, N;
  cin >> K>> N;
  vector<int> v(N);
  vector<char> w(N);
  for (size_t i = 0; i < N; i++)
  {
    cin >> v[i]>> w[i];
  }

  // 計算


  return 0;
}
